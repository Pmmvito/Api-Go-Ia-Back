# Database Configuration
DATABASE_DSN=host=localhost user=postgres password=sua_senha dbname=seu_db port=5432 sslmode=disable TimeZone=America/Sao_Paulo

# JWT Configuration
JWT_SECRET=sua-chave-secreta-super-segura-mude-em-producao

# Email Configuration (SMTP)
# Para Gmail: Use uma "Senha de App" (não sua senha normal)
# 1. Ative 2FA na sua conta Google
# 2. Acesse: https://myaccount.google.com/apppasswords
# 3. Crie uma senha de app para "Mail"
# 4. Use essa senha gerada aqui
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-de-app-do-gmail
SMTP_SENDER_NAME=Sistema de Notas Fiscais

# Para Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_EMAIL=vitor.benevento@edu.unipar.br
# SMTP_PASSWORD=mili0070

# Para outros provedores, consulte a documentação do provedor

# Gemini API Configuration
GEMINI_API_KEY=sua-chave-api-do-gemini
GEMINI_MODEL=gemini-2.5-flash-preview-05-20

# AI Worker Pool Configuration (para Gemini 2.5 Flash Free Tier)
# Gemini 2.5 Flash Preview tem limite de ~10 RPM (requests por minuto)
# - MAX_AI_WORKERS: Número de workers simultâneos (recomendado: 3 para 10 RPM)
# - AI_QUEUE_SIZE: Tamanho da fila de espera (recomendado: 50)
MAX_AI_WORKERS=3
AI_QUEUE_SIZE=50

# Server Configuration
PORT=8080
