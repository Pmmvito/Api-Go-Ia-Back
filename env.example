# Configuração do Banco de Dados PostgreSQL
DATABASE_DSN=postgresql://usuario:senha@localhost:5432/nome_do_banco?sslmode=disable

# Chave secreta para JWT (ALTERE PARA UM VALOR SEGURO EM PRODUÇÃO)
JWT_SECRET=sua-chave-secreta-super-segura-mude-isso-em-producao

# Chave da API do Google Gemini para análise de notas fiscais
# Obtenha sua chave em: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=sua-chave-api-do-gemini-aqui

# Modelo do Gemini a ser usado (opcional, padrão: gemini-1.5-flash)
# Opções: gemini-1.5-flash, gemini-1.5-pro, gemini-2.5-flash-preview-05-20
GEMINI_MODEL=gemini-1.5-flash

# Custos por 1.000 tokens (em centavos de R$) - usado para tracking de custos
# Valores padrão para gemini-1.5-flash: 10 centavos/1K (prompt) e 20 centavos/1K (response)
GEMINI_PROMPT_COST_PER_1K_CENTS=10.0
GEMINI_RESPONSE_COST_PER_1K_CENTS=20.0

# Limite de tokens de IA por usuário (0 = sem limite)
# Quando atingido, usuário não poderá usar endpoints que consomem IA
AI_TOKEN_LIMIT_PER_USER=0
