name: Deploy API Go no Servidor Ubuntu

on:
  push:
    branches:
      - main  # Ou a branch que você usa
  workflow_dispatch:  # Permite execução manual

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📋 Informações do Deploy
        run: |
          echo "🚀 Iniciando deploy da API Go"
          echo "📦 Commit: ${{ github.sha }}"
          echo "� Autor: ${{ github.actor }}"
          echo "📝 Mensagem: ${{ github.event.head_commit.message }}"
          echo "⏰ Data: $(date)"

      - name:  Conectar via SSH e fazer Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 10m

          script: |
            # Não usa set -e para permitir tratamento de erros customizado
            
            echo "================================================"
            echo "🔍 ETAPA 1: Verificando ambiente"
            echo "================================================"
            echo "📂 Diretório atual: $(pwd)"
            echo "👤 Usuário: $(whoami)"
            echo "🐹 Versão Go: $(go version)"
            
            echo ""
            echo "================================================"
            echo "📁 ETAPA 2: Navegando para o projeto"
            echo "================================================"
            cd /DATA/Downloads/Api-Go-Ia-Back
            echo "✅ Diretório do projeto: $(pwd)"
            
            echo ""
            echo "================================================"
            echo "📥 ETAPA 3: Atualizando código do repositório"
            echo "================================================"
            echo "🔄 Branch atual: $(git branch --show-current)"
            echo "📌 Commit atual (antes): $(git rev-parse --short HEAD)"
            
            # Verifica se há mudanças locais
            if ! git diff-index --quiet HEAD --; then
              echo "⚠️ Detectadas mudanças locais, fazendo stash..."
              git stash save "Auto-stash antes do deploy $(date)"
            fi
            
            git fetch origin main
            git reset --hard origin/main
            echo "📌 Commit atual (depois): $(git rev-parse --short HEAD)"
            echo "✅ Código atualizado com sucesso"
            
            echo ""
            echo "================================================"
            echo "📦 ETAPA 4: Gerenciando dependências"
            echo "================================================"
            go mod tidy
            echo "✅ Dependências verificadas"
            
            echo ""
            echo "================================================"
            echo "🔨 ETAPA 5: Compilando a aplicação"
            echo "================================================"
            echo "🏗️ Iniciando compilação..."
            go build -v -o api-go-ia main.go
            echo "✅ Compilação concluída com sucesso"
            
            echo ""
            echo "================================================"
            echo "📊 ETAPA 6: Verificando binário"
            echo "================================================"
            ls -lh api-go-ia
            echo "✅ Binário verificado"
            
            echo ""
            echo "================================================"
            echo "🔄 ETAPA 7: Reiniciando o serviço"
            echo "================================================"
            
            # Tenta parar o serviço com sudo (se disponível)
            echo "⏸️ Tentando parar serviço..."
            if command -v sudo &> /dev/null && sudo -n systemctl stop api-go-ia.service 2>/dev/null; then
              echo "✅ Serviço parado com systemctl"
              
              echo "▶️ Iniciando serviço..."
              sudo systemctl start api-go-ia.service
              
              echo "⏳ Aguardando 3 segundos..."
              sleep 3
              
              echo "🔍 Verificando status do serviço..."
              sudo systemctl status api-go-ia.service --no-pager || true
            else
              echo "⚠️ sudo não disponível ou serviço não gerenciado por systemd"
              echo "🔄 Tentando reiniciar manualmente..."
              
              # Mata processo existente usando PID file se existir
              echo "🛑 Parando processos existentes..."
              if [ -f /tmp/api-go-ia.pid ]; then
                OLD_PID=$(cat /tmp/api-go-ia.pid)
                if kill -0 $OLD_PID 2>/dev/null; then
                  echo "   Matando processo PID: $OLD_PID"
                  kill -9 $OLD_PID 2>/dev/null || true
                  sleep 1
                fi
                rm -f /tmp/api-go-ia.pid
              fi
              
              # Fallback: mata qualquer processo api-go-ia (exceto grep)
              if pgrep -f "./api-go-ia" > /dev/null 2>&1; then
                echo "   Matando processos restantes..."
                pkill -9 -f "./api-go-ia" 2>/dev/null || true
                sleep 2
              else
                echo "   Nenhum processo anterior encontrado"
              fi
              
              # Inicia novo processo em background
              echo "▶️ Iniciando novo processo..."
              nohup ./api-go-ia > /tmp/api-go-ia.log 2>&1 &
              NEW_PID=$!
              echo $NEW_PID > /tmp/api-go-ia.pid
              
              echo "✅ Processo iniciado (PID: $NEW_PID)"
              echo "⏳ Aguardando 5 segundos para validação..."
              sleep 5
              
              # Mostra as primeiras linhas do log
              echo ""
              echo "📋 Primeiras linhas do log da aplicação:"
              echo "----------------------------------------"
              head -n 20 /tmp/api-go-ia.log 2>/dev/null || echo "Log ainda não disponível"
              echo "----------------------------------------"
              echo ""
              
              # Verifica se o processo está rodando
              if kill -0 $NEW_PID 2>/dev/null; then
                echo "✅ Processo está rodando"
                echo "📊 Informações do processo:"
                ps -p $NEW_PID -o pid,ppid,cmd,etime 2>/dev/null || true
              else
                echo "❌ ERRO: Processo não está rodando"
                echo "📋 Log completo (últimas 50 linhas):"
                tail -n 50 /tmp/api-go-ia.log 2>/dev/null || echo "Log não disponível"
                exit 1
              fi
            fi
            
            echo ""
            echo "================================================"
            echo "✅ ETAPA 8: Validação final"
            echo "================================================"
            
            # Verifica se systemctl está disponível com sudo
            if command -v sudo &> /dev/null && sudo -n systemctl is-active --quiet api-go-ia.service 2>/dev/null; then
              echo "✅ Serviço systemd está ATIVO"
              echo "🎉 Deploy concluído com SUCESSO!"
            elif [ -f /tmp/api-go-ia.pid ]; then
              PID=$(cat /tmp/api-go-ia.pid)
              if kill -0 $PID 2>/dev/null; then
                echo "✅ Processo manual está RODANDO (PID: $PID)"
                echo "📊 Processos ativos:"
                ps -p $PID -o pid,ppid,cmd,etime 2>/dev/null || true
                echo "🎉 Deploy concluído com SUCESSO!"
              else
                echo "❌ ERRO: Processo do PID file não está ativo"
                echo "📋 Tentando obter logs..."
                tail -n 50 /tmp/api-go-ia.log 2>/dev/null || echo "Log não disponível"
                exit 1
              fi
            else
              echo "❌ ERRO: Nem systemd nem processo manual detectado"
              echo "📋 Tentando obter logs..."
              tail -n 50 /tmp/api-go-ia.log 2>/dev/null || echo "Log não disponível"
              exit 1
            fi
            
            echo ""
            echo "================================================"
            echo "📊 RESUMO DO DEPLOY"
            echo "================================================"
            echo "✅ Commit: $(git rev-parse --short HEAD)"
            echo "✅ Data: $(date)"
            echo "✅ Status: SUCCESS"
            
            # Mostra últimas linhas do log da aplicação
            if [ -f /tmp/api-go-ia.log ]; then
              echo ""
              echo "📋 Últimas 10 linhas do log da aplicação:"
              echo "----------------------------------------"
              tail -n 10 /tmp/api-go-ia.log 2>/dev/null || true
              echo "----------------------------------------"
            fi
            
            echo "================================================"

      - name: ✅ Deploy Concluído
        if: success()
        run: |
          echo "🎉 Deploy realizado com sucesso!"
          echo "🌐 API disponível em: http://${{ secrets.SERVER_HOST }}:61489"

      - name: ❌ Deploy Falhou
        if: failure()
        run: |
          echo "❌ Deploy falhou! Verifique os logs acima."
          exit 1
