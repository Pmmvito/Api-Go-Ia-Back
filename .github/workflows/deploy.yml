name: Deploy API Go no Servidor Ubuntu

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📋 Informações do Deploy
        run: |
          echo "🚀 Iniciando deploy da API Go"
          echo "📦 Commit: ${{ github.sha }}"
          echo "👤 Autor: ${{ github.actor }}"
          echo "📝 Mensagem: ${{ github.event.head_commit.message }}"
          echo "⏰ Data: $(date)"

      - name: 🚚 Conectar via SSH e fazer Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 10m

          script: |
            # 1. Navegue até a pasta do projeto
            cd /DATA/Downloads/Api-Go-Ia-Back
            
            # 2. Puxe as últimas alterações
            echo "📥 Baixando atualizações..."
            git fetch origin main
            git reset --hard origin/main
            
            # 3. Baixe dependências
            echo "📦 Baixando dependências..."
            go mod tidy
            
            # 4. Compile o novo binário
            echo "🔨 Compilando a aplicação Go..."
            go build -o api-go-ia main.go
            
            # 5. Reinicie o serviço (systemd faz todo o trabalho)
            echo "▶️ Reiniciando o serviço..."
            sudo systemctl restart api-go-ia.service
            
            echo "🎉 Deploy finalizado!"